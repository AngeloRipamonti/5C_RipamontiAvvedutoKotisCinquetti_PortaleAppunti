import {generateUserPresenter} from "../presentation/userPresenter.js";

export const generateUser = (parentElement, pubSub) => {

    //variables

    
    return{
        render: function(info) {

        }
    }
}