import {generateNote} from "../presentation/note.js";

export const generateViewNote = (parentElement, pubSub) => {

    //variables

    
    return{
        render: function(note) {

        }
    }
}