import { generateDocument } from "../model/document";

export const generatePostManager = (parentElement) => {

    let document;
    let feed;
    
    return{
        updateFeed: function() {

        },
        viewPost: function(){
            
        }
    }
}